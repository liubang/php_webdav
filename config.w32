// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("webdav", "for webdav support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("webdav", "enable webdav support", "no");

if (PHP_WEBDAV != "no") {
	EXTENSION("webdav", "webdav.c");
}

